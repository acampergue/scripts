# scripts
Scripts utilitaires perso


# Install logs
```
   1  bash ./run_upgrader.sh 
    2  ./vmware-install.pl 
    3  sudo ./vmware-install.pl 
    4  sudo ./run_upgrader.sh 
    5  apt-get update
    6  apt-get upgrade
    7  apt-get dist-upgrade
    8  apt install chromium-browser
    9  apt install zsh terminator htop ansible
   10  sh -c "$(curl -fsSL https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"
   11  apt install git
   12  sh -c "$(curl -fsSL https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"
   13  history


    1  apt install vim 
    2  vi ~/.zshrc
    3  sudo apt-get remove docker docker-engine docker.io
    4  sudo apt-get install \\n    apt-transport-https \\n    ca-certificates \\n    curl \\n    software-properties-common
    5  curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -
    6  lsb_release -cs
    7  sudo add-apt-repository \\n   "deb [arch=amd64] https://download.docker.com/linux/ubuntu \\n   xenial \\n   stable"
    8  sudo apt-get update
    9  sudo apt-get install docker-ce
   10  sudo curl -L https://github.com/docker/compose/releases/download/1.21.2/docker-compose-$(uname -s)-$(uname -m) -o /usr/local/bin/docker-compose
   11  sudo curl -L https://github.com/docker/compose/releases/download/1.21.2/docker-compose-$(uname -s)-$(uname -m) -o /usr/local/bin/docker-composesudo chmod +x /usr/local/bin/docker-compose
   12  sudo chmod +x /usr/local/bin/docker-compose
   13  docker-compose
   14  docker-compose version
   15  docker version 
   16  docker-composesudo
   17  docker-compose
   18  cd
   19  ll
   20  mkdir bin 
   21  cd Templates
   22  l
   23  cd ..
   24  mv Downloads/terraform_0.11.7_linux_amd64.zip bin
   25  cd bin
   26  l
   27  unzip terraform_0.11.7_linux_amd64.zip
   28  ll
   29  cd
   30  vim .zshrc
   31  zsh
   32* apt install tmux 
```
